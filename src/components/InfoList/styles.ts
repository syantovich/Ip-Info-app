import styled from "styled-components";

export const WrapperList = styled.div({
  width: 400,
  padding: 30,
  backgroundColor: "rgba(176,170,170,0.56)",
});
export const CurrentIp = styled.div({
  height: 300,
});
